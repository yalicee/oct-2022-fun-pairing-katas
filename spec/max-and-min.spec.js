const { max, min } = require('../katas/max-and-min');

describe('max', () => {
  it('describe what happens if this test succeeds here', () => {
    // expect(max(???)).to...
  });
});

describe('min', () => {
  it('describe what happens if this test succeeds here', () => {
    // expect(min(???)).to...
  });
});
