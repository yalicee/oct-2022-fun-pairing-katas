const caesarCipher = require('../katas/caesar-cipher');

describe('caesarCipher', () => {
  it('describe what happens if this test succeeds here', () => {
    // expect(caesarCipher()).to...
  });
});
